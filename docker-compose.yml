services:
  dashboard-db:
    image: postgres:16
    container_name: dashboard-db
    restart: always
    env_file:
      - backend/.env
    ports:
      - '5432:5432'
    volumes:
      - dashboard_postgres_data:/var/lib/postgresql/data

volumes:
  dashboard_postgres_data:
